import React from "react";
import { View } from "react-native";
import { Pet } from "../../entities/pet";

const PetDetails: React.FC<Pet> = (pet) => {
  return <View />;
};

export default PetDetails;
